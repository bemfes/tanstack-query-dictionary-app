import type { FC } from "react"
import ThemeSwitcher from "../ThemeSwitcher"

const Header: FC = () => {
    return (
        <header>
           <ThemeSwitcher/> 
        </header>
    )
}

export default Header
